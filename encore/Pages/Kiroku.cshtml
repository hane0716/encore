@page
@model encore.Pages.KirokuModel
@{
    Layout = "_Layout";
}
<title>@Model.Title</title>


<h1>ライブを記録する</h1>
@if (!string.IsNullOrEmpty(Model.KaiinMessage))
{
    <p>@Model.KaiinMessage</p>
}


<form method="post" asp-page-handler="Insert_Kiroku" style="display:flex; gap:8px; align-items:center;">
    <label>ライブ名：</label>
    <input asp-for="KirokuName" required />
    <input asp-for="LiveDate" type="date" required />
    <button type="submit">ライブ名を登録</button>
</form>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <p>@Model.Message</p>
}

<h2>登録済みのライブ一覧</h2>
@if (Model.KirokuList.Any())
{
    <table>
        <thead>
            <tr>
                <th>日付</th>
                <th>ライブ名</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.KirokuList)
            {
                <tr>
                    <td>@item.Date.ToString("yyyy-MM-dd")</td>
                    <td>@item.Name</td>
                </tr>
               
@*                 <tr>
                    <td>@item.Date.ToString("yyyy-MM-dd")</td>
                    <td>@item.Name</td>
                    <td style="position:relative;">
                        <button class="menu-button" onclick="toggleMenu(@item.Id)">⋯</button>
                        <div class="menu-popup" id="menu-@item.Id" style="display:none;">
                            <form method="get" asp-page="/EditKiroku">
                                <input type="hidden" name="id" value="@item.Id" />
                                <button type="submit">編集</button>
                            </form>
                            <button onclick="confirmDelete(@item.Id)">削除</button>
                        </div>
                    </td>
                </tr>
 *@
            }
        </tbody>
@*         <script>
            function toggleMenu(id) {
                const menu = document.getElementById(`menu-${id}`);
                const allMenus = document.querySelectorAll('.menu-popup');
                allMenus.forEach(m => { if (m !== menu) m.style.display = 'none'; });
                menu.style.display = (menu.style.display === 'none') ? 'block' : 'none';
            }

            function confirmDelete(id) {
                if (confirm("本当に削除しますか？")) {
                    const form = document.createElement("form");
                    form.method = "post";
                    form.action = "?handler=Delete_Kiroku";

                    const input = document.createElement("input");
                    input.type = "hidden";
                    input.name = "KirokuId";
                    input.value = id;

                    form.appendChild(input);
                    document.body.appendChild(form);
                    form.submit();
                }
            }
        </script>
 *@
    </table>
}
else
{
    <p>まだライブ記録はありません。</p>
}


