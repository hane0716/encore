<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background-color: #2c2f33; /* 深いグレー */
            color: #e3e3e3;
        }

        .container {
            display: flex;
            height: 100vh;
        }

        .sidebar {
            width: 220px;
            background-color: #3a3f47; /* ネイビー寄りのグレー */
            padding: 20px;
            box-shadow: 2px 0 8px rgba(0, 0, 0, 0.3);
        }

            .sidebar h3 {
                color: #ffffff;
                margin-bottom: 20px;
                font-size: 1.2em;
            }

            .sidebar a {
                display: block;
                margin-bottom: 12px;
                text-decoration: none;
                color: #a3d5ff; /* 明るめの青で優しい印象 */
                font-weight: 500;
                transition: color 0.3s;
            }

                .sidebar a:hover {
                    color: #ffffff;
                }

        .main {
            flex-grow: 1;
            padding: 30px;
            background-color: #36393f; /* やや明るめのグレーで安心感 */
        }

/*         .menu-button {
            background: none;
            border: none;
            font-size: 20px;
            cursor: pointer;
            color: #ccc;
        }

        .menu-popup {
            position: absolute;
            right: 0;
            top: 30px;
            background-color: #2c2f33;
            border: 1px solid #444;
            padding: 8px;
            z-index: 100;
            display: flex;
            flex-direction: column;
        }

            .menu-popup button {
                background: none;
                border: none;
                color: #a3d5ff;
                padding: 4px 8px;
                text-align: left;
                cursor: pointer;
            }

                .menu-popup button:hover {
                    background-color: #3a3d42;
                }
 */
    </style>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <h3>メニュー</h3>
            <a asp-page="/Loginmae">会員登録</a>
            @* <a asp-page="/Logingo">会員画面</a> *@
            @* <a asp-page="/Live">ライブ記録</a> *@
            <a asp-page="/Admin">管理画面</a>
        </div>
        <div class="main">
            @RenderBody()
        </div>
    </div>
</body>
</html>
